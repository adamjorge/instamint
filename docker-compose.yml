version: "3.8"

services:
  db:
    container_name: db
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=instamint-local
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=password
